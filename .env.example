# This monorepo uses separate env files:
#   client/.env.example  → Frontend (Vite)
#   server/.env.example  → Backend (Express)
#
# Copy each to .env / .env.local and fill values. Never commit real secrets.
